/*! dsh-taskfold client bundle — GENERATED by scripts/build-client.mjs; do not edit.
    Single source of truth: plugins/task-stack-ui.mjs is spliced into the body. */
window.__ModuleLoader__.load({
	id: "__PACKAGE_ID__",
	factory: (require) => {
		var module = { exports: {} };
		var exports = module.exports;
		Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
		let react = require("react");
		//#region inlined: plugins/task-stack-ui.mjs (must stay import-free)
__TASKFOLD_MODEL_SOURCE__
		//#endregion
		const TaskStackDock = makeTaskStackDock(react);
		/** Required services: the host slot registry (minimal, like TodoDock). */
		const inject = ["slots"];
		/** Mount the task-stack dock beside the shipped docks (todo:0 goal:10 queue:20). */
		function apply(ctx) {
			// Plugin CSS is not part of the shell stylesheet: every client bundle
			// injects its own, keyed by data-plugin-css (same as first-party ones).
			injectTaskStackCss();
			ctx.slots.inject("conversation.input.dock", () => ctx.slots.register({
				name: "conversation.input.dock",
				id: "__DOCK_ID__",
				order: 15
			}, TaskStackDock));
		}
		exports.apply = apply;
		exports.inject = inject;
		return module.exports;
	}
});